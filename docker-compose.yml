services:
  proxy:
    build: .
    ports:
      - "11434:11434"
    volumes:
      - ~/.codex:/root/.codex:ro
    environment:
      - PORT=11434
      - AUTH_PATH=/root/.codex/auth.json
      - LOG_LEVEL=info
    restart: unless-stopped
